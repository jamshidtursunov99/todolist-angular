import { FormatDeadlinePipe } from './format-deadline.pipe';

describe('FormatDeadlinePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDeadlinePipe();
    expect(pipe).toBeTruthy();
  });
});
